c# winform
1、在打开其他全屏或无边框窗口应用程序的情况下，
	显示文字(其他界面为透明)
	静态显示（事件更新y）
	Install-Package SharpDX
	Install-Package SharpDX.Direct2D1
	Install-Package SharpDX.Direct3D9
	
2、当焦点为其他全屏或无边框窗口应用程序的情况下，按下预设的快捷键按键（以小键盘方向键为例），执行对应的程序
3、按下预设的快捷键（例如方向键左）后，用户可以在屏幕上左键点击两个点，这两个点以小圆点显示在屏幕上，并获取这两个点之间的屏幕距离

4、根据已有的地图比例，标记两个点（显示出来，按下方向键右清除），获取这两个点的距离和角度

5、根据选定的火炮类型，计算扩散半径，显示图形
6、根据输入的风向，输出修正后的角度方向

7、程序启动后会在系统托盘显示图标