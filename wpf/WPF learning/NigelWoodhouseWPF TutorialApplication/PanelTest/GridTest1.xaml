<Window x:Class="PanelTest.GridTest1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PanelTest"
        mc:Ignorable="d"
        Title="GridTest1" Height="450" Width="800">
    <Grid ShowGridLines="False" Margin="0,0,0,-5" UseLayoutRounding="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height= "*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height= "*"></RowDefinition>
            <RowDefinition Height= "*"></RowDefinition>
        </Grid.RowDefinitions>
        <GridSplitter Grid.Row="0" Grid.RowSpan="4" Grid.Column="1" Width="10" HorizontalAlignment="Center" Background="Red"></GridSplitter>
        <GridSplitter Grid.Row="1" Grid.ColumnSpan="4" Grid.Column="0" Height="10" HorizontalAlignment="Stretch" Background="Blue"></GridSplitter>

        <Button x:Name="BtnMainWindow" Grid.Column="0" Grid.Row="0" Style="{StaticResource OKStyle}" Click="BtnMainWindow_Click" MouseEnter="BtnMainWindow_MouseEnter">MainWindow</Button>
        <Button Grid.Column="2" Grid.Row="0" Style="{StaticResource NGStyle}">World</Button>
        <Button Grid.Column="0" Grid.Row="2" Content="FFAD" Background="DarkViolet" BorderThickness = "6" BorderBrush="Lime"  FontSize="40">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                        <TextBlock x:Name="txtBlock" Text="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="border" Property="Background" Value="Purple"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="border" Property="Background" Value="Gray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>

            </Button.Template>
        </Button>
        <Button Grid.Column="2" Grid.Row="2" >Y</Button>
        <InkCanvas Grid.Column="0" Grid.Row="3" />
    </Grid>
</Window>
